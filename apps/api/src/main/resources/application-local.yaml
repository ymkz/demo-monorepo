spring:
  config:
    import: optional:configtree:/tmp/ymkz/demo-monorepo/configtree/
  web:
    resources:
      add-mappings: true
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db
    username: demo_user
    password: ${DEMODB_DEMOUSER_PASSWORD}
