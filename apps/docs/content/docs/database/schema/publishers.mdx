---
title: "publishers テーブル"
description: "出版社情報テーブル定義"
---

# publishers テーブル

## 概要

出版社の情報を格納するテーブルです。

## カラム定義

| カラム名 | 型 | NULL | デフォルト | 説明 |
|----------|------|------|------------|------|
| id | int | NO | auto_increment | 出版社ID（自動採番） |
| publisher_name | varchar(128) | NO | - | 出版社名 |
| created_at | datetime | NO | CURRENT_TIMESTAMP | レコード作成時刻 |
| updated_at | datetime | YES | ON UPDATE CURRENT_TIMESTAMP | レコード更新時刻 |
| deleted_at | datetime | YES | - | レコード論理削除時刻 |

## 制約

| 制約名 | 種別 | 定義 |
|--------|------|------|
| PRIMARY | PRIMARY KEY | PRIMARY KEY (id) |

## インデックス

| インデックス名 | 定義 |
|----------------|------|
| PRIMARY | PRIMARY KEY (id) USING BTREE |

## 関連テーブル

| 関係 | テーブル | 説明 |
|------|----------|------|
| 1対多 | [books](/docs/database/schema/books) | 出版社が複数の書籍を持つ |

## CREATE TABLE

```sql
CREATE TABLE `publishers` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '出版社ID（自動採番）',
  `publisher_name` varchar(128) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '出版社名',
  `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT 'レコード作成時刻',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT 'レコード更新時刻',
  `deleted_at` datetime DEFAULT NULL COMMENT 'レコード論理削除時刻',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='出版社の情報を格納するテーブル'
```

## ER図

![publishersテーブルER図](/images/schema/publishers.svg)

---

> Generated by [tbls](https://github.com/k1LoW/tbls)
