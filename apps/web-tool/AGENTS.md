# WEB-TOOL APP

**Generated:** 2026-03-29
**Parent:** /AGENTS.md

## OVERVIEW
TanStack Start application with TanStack Router file-based routing and Tailwind CSS.

## STRUCTURE
```
apps/web-tool/
├── src/
│   ├── router.tsx          # TanStack Router configuration
│   ├── routeTree.gen.ts    # Auto-generated route tree
│   ├── routes/             # File-based routes
│   │   ├── __root.tsx      # Root document
│   │   ├── index.tsx       # Search page
│   │   └── books/
│   │       └── download.ts # CSV download proxy route
│   └── generated/          # Auto-generated from OpenAPI
│       ├── client/         # API client
│       └── core/           # Core types
├── vite.config.ts          # Vite + TanStack Start plugin
└── package.json
```

## WHERE TO LOOK

| Task | Location | Notes |
|------|----------|-------|
| Router | `src/router.tsx` | TanStack Router configuration |
| Routes | `src/routes/` | File-based TanStack Router routes |
| Root Layout | `src/routes/__root.tsx` | Document shell and metadata |
| API Client | `src/generated/client/` | Auto-generated from backend OpenAPI |
| Config | `openapi-ts.config.ts` | OpenAPI codegen configuration |
| Styles | `src/style.css` | Tailwind CSS |

## CONVENTIONS

### TanStack Start Patterns
- Routes are file-based under `src/routes/`
- Root document lives in `src/routes/__root.tsx`
- Router configuration lives in `src/router.tsx`
- `src/routeTree.gen.ts` is generated by TanStack Router and must not be edited manually
- Type-safe search params use Zod schemas with `validateSearch`

### API Routes
- Use TanStack Start server handlers under `src/routes/`
- Proxy to backend via fetch()
- Environment variables for tokens (API_TOKEN)

### Development
- Port 4000 (via `vite dev`)
- Vite for fast HMR
- Biome for linting (run `pnpm lint`)

## ANTI-PATTERNS

| Pattern | Why Forbidden |
|---------|---------------|
| Hardcoded secrets | Use environment variables |
| Manual API types | Use generated OpenAPI types instead |
| Editing routeTree.gen.ts | Generated by TanStack Router |
